.side-bar
  .side-header
    .side-header__box
      %p.side-header__box__user-name
        = current_user.name
      %ul.side-header__box__menu
        %li.side-header__box__edit-user
          = link_to "#" do
            = icon('fas', 'edit')
        %li.side-header__box__config
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog')
  .groups
    =link_to "#" do
      .group
        %p.group__group-name techcamp
        %p.group__latest-message まだメッセージがありません
    =link_to "#" do
      .group
        %p.group__group-name test
        %p.group__latest-message none